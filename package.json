{
  "name": "nexus-scarlet-peach-catalog",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "node src/server.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler check",
    "build:snapshot": "node scripts/build-snapshot.js",
    "test": "node --test"
  }
}
